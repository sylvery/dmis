{% extends 'base.html.twig' %}

{% block title %}Edit Treatment{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>Update Treatment Details</h1>
            </div>
            <div class="col-md-12">
                {{ include('treatment/_form.html.twig', {'button_label': 'Update'}) }}
                <a class="btn btn-secondary" href="{{ path('treatment_index') }}">back to list of treatments</a>
            </div>
        </div>
    </div>
{% endblock %}
