<form method="post" action="{{ path('stock_delete', {'id': stock_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stock_item.id) }}">
    <button class="btn">Delete</button>
</form>
